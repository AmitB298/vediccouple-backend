const Joi = require('joi');module.exports = (req, res, next) => {  const schema = Joi.object({    code: Joi.string().pattern(/^VCPL-\d{4}$/).required(),    userId: Joi.string().required(),    expiresAt: Joi.date().iso()  });  const { error } = schema.validate(req.body);  if (error) {    return res.status(400).json({ success: false, message: error.details[0].message });  }  next();};

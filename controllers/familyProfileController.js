const FamilyProfile = require('../models/FamilyProfile');exports.createFamilyProfile = async (req, res) => {  try {    const profile = new FamilyProfile(req.body);    await profile.save();    res.status(201).json({ success: true, data: profile });  } catch (error) {    res.status(500).json({ success: false, message: error.message });  }};exports.getFamilyProfile = async (req, res) => {  try {    const profile = await FamilyProfile.findById(req.params.id);    if (!profile) return res.status(404).json({ success: false, message: 'Not found' });    res.json({ success: true, data: profile });  } catch (error) {    res.status(500).json({ success: false, message: error.message });  }};

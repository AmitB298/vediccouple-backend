const inviteCodeController = require('./inviteCodeController');const InviteCode = require('../models/InviteCode');jest.mock('../models/InviteCode');describe('InviteCodeController', () => {  test('should create invite code', async () => {    InviteCode.prototype.save = jest.fn().mockResolvedValue({ code: 'VCPL-1234' });    const req = { body: { code: 'VCPL-1234', userId: 'user123' } };    const res = { status: jest.fn().mockReturnThis(), json: jest.fn() };    await inviteCodeController.createInviteCode(req, res);    expect(res.status).toHaveBeenCalledWith(201);  });  test('should validate invite code - not found', async () => {    InviteCode.findOne.mockResolvedValue(null);    const req = { params: { code: 'VCPL-9999' } };    const res = { status: jest.fn().mockReturnThis(), json: jest.fn() };    await inviteCodeController.validateInviteCode(req, res);    expect(res.status).toHaveBeenCalledWith(404);  });});
